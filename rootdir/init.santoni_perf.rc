# Santoni 2GB RAM Base Tuning
# This runs on boot, then Spectrum profiles take over
on property:sys.boot_completed=1
    # Base VM tuning (Spectrum profiles will override these)
    write /proc/sys/vm/swappiness 60
    write /proc/sys/vm/dirty_ratio 10
    write /proc/sys/vm/dirty_background_ratio 5
    write /proc/sys/vm/vfs_cache_pressure 50

    # Set I/O scheduler to BFQ for eMMC
    write /sys/block/mmcblk0/queue/scheduler bfq
    write /sys/block/mmcblk0/queue/read_ahead_kb 128

    # Disable readahead for zRAM
    write /sys/block/zram0/queue/read_ahead_kb 0

    # Apply default Spectrum profile (Balance)
    setprop persist.spectrum.profile 0
